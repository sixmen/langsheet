_title: swap
C:
  body: |-
    tmp = x;
    x = y;
    y = tmp;
C++:
  body: |-
    \preproc{#include <algorithm>\}
    std::swap(x, y);
Java:
  body: |-
    tmp = x;
    x = y;
    y = tmp;
JavaScript:
  body: |-
    x = [y, y = x][\number{0\}];
Perl:
  body: |-
    ($x, $y) = ($y, $x);
PHP:
  body: |-
    list($x, $y) = array($y, $x);
